# MLflow configuration
tracking_uri: "http://127.0.0.1:8080"
experiment_name: "emotion_recognition"
run_name: "lstm_audio_emotion"

# Artifact logging
log_artifacts: true
artifact_path: "artifacts"
log_model: true
log_params: true
log_metrics: true

# Git integration
log_git_commit: true
log_git_repo: true
